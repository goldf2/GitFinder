# 2.0.0-alpha.28 群组标题与资源库侧栏

## 本次范围

- 正式白板的群组名称、成员数与编辑按钮在屏幕上保持固定大小，锚定组框左上方；反向补偿视图缩放，不改变群组边界、成员坐标或关系。
- 群组名称固定为 14px，成员数 10px；普通卡片仍随画布缩放。群组内备注仍属于画布内容。
- 资源库移除原浮动面板的边框和大标题，改为与侧栏一致的 11px 灰色标题、折叠箭头和间距。移动按钮在标题悬停或键盘聚焦时显示，保留左右停靠与折叠偏好。

## 验证

- 新增两项回归，修改前均失败、修改后通过：25%–250% 缩放反向补偿且不改坐标；资源库标题、箭头和移动入口。
- `node --test test/relationship-board-ui.test.js`：97/97 通过。
- `npm run check`：764/764 通过；194 个 JavaScript 文件语法检查通过。
- `npm run pack`：macOS arm64 开发包通过构建和产物门禁。
- 正式控制器隔离页面：100% 与 83% 时两组标题屏幕高度均为 18.1953125px，卡片文字正常缩小；截图确认标签贴着组框、侧栏标题一致、折叠箭头改变方向。
- 本机安装 `/Applications/GitFinder 2 Alpha.app`，签名校验通过，图标与 `public/icon.icns` 一致，界面显示 v2.0.0-alpha.28。
- 本机 Coolify 同步后恢复 1 台服务器、21 个部署和白板 45 个节点、44 条关系；资源库可展开，验证后恢复原折叠状态。
- 上一安装备份：`/tmp/gitfinder2-alpha28-install.JBPrw0/GitFinder 2 Alpha.previous.app`。

## 访问点“未知”的诊断（本次未实现检测）

`src/shared/panelTopologyProjection.js` 将部署域名投影为访问点时，固定设置 `status: 'unknown'`、`latencyMs: null`，时间沿用部署的 `observedAt`。当前 Coolify 读取路径没有执行站点 HTTP 可用性或延迟探测。

因此“未知”表示尚无访问点检测数据，不能解释为网站故障，也不能用部署运行状态直接代替网站可访问状态。后续如增加探测，状态和时间必须使用独立的探测结果。

未提交、未推送。本机包为 ad-hoc 签名 Alpha 开发包。
