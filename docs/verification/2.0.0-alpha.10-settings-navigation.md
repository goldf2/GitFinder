# GitFinder 2.0.0-alpha.10 设置分类导航

## 交付范围

- 应用设置改为稳定的左侧分类导航，右侧一次只显示一个设置分类，不再纵向瀑布式展开全部内容。
- 分类包含：目录显示、侧边栏、外观、Panel 数据源、开发工具、项目身份。
- 保留 Panel 与开发工具入口的分类深链接；切换分类不重新渲染表单，因此未保存输入不会丢失。
- 使用纵向 Tab 语义、可见焦点和方向键/Home/End 导航；窄窗口保留左侧结构并压缩辅助说明。
- 设置主体独立滚动，页头、左侧分类与底部保存操作保持稳定。

## 自动验证

- `npm test`：677 项通过，0 项失败。
- `npm run check`：通过；JavaScript 语法检查覆盖 190 个文件。
- `npm run verify:release-source`：development 源码门禁通过。
- `npm run pack`：development 产物门禁通过；开发包为 ad-hoc 签名，不具备正式分发资格。
- `git diff --check`：通过。

## 本机安装验证

- 安装位置：`/Applications/GitFinder 2 Alpha.app`
- 包内版本：`2.0.0-alpha.10`
- `codesign --verify --deep --strict`：通过。
- 应用正常启动，界面版本显示 `v2.0.0-alpha.10`。
- 从工具栏打开“应用设置”后，左侧六个分类均可见，右侧只显示“目录显示”。
- 点击“Panel 数据源”后，选中态与右侧内容同步切换，其余分类内容不再出现在可访问树中。
- 验收结束时应用保留在“目录显示”分类，便于继续人工检查。

## 构建产物

- 文件：`dist/GitFinder-2-2.0.0-alpha.10-arm64-mac.zip`
- 大小：125,808,548 bytes
- SHA-256：`2a8a75d908f24225f70cc0cfc3927c3f62acd9103dfc0e3d2064e1b8a1eb9681`

## 发布状态

- 已实现、已自动验证、已构建、已安装并完成本机可见验收。
- 尚未提交或推送；当前仓库未配置远程，本次也未收到推送指令。
