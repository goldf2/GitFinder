# GitFinder 2.0.0-alpha.12 设置页紧凑标题区

## 交付范围

- 移除设置页内重复的 `GitFinder 2` 品牌行。
- 将“应用设置”从 26 px 降为 20 px 的工具页标题。
- 宽窗口中标题与本机配置说明同排，窄窗口中自动换行。
- 页面顶部留白由 28 px 降为 16 px，标题区下方留白由 20 px 降为 12 px。
- 不改变设置分类、表单、保存与取消逻辑。

## 自动验证

- 先增加紧凑标题区回归测试并确认旧实现失败，修改后通过。
- `npm run check`：679 项测试通过，0 项失败；JavaScript 语法检查覆盖 190 个文件。
- `npm run verify:release-source`：development 源码门禁通过。
- `npm run pack`：development 产物门禁通过。
- `git diff --check`：通过。

## 本机可见验收

- 安装位置：`/Applications/GitFinder 2 Alpha.app`
- 包内及界面版本：`2.0.0-alpha.12`
- `codesign --verify --deep --strict`：通过。
- 应用正常启动，设置入口可打开紧凑标题区。
- 实际窗口中标题与说明同排，设置分类和首行内容随即进入可视区域，没有重复品牌行。
- 窄窗口换行行为由响应式样式和回归断言覆盖。

## 构建产物

- 文件：`dist/GitFinder-2-2.0.0-alpha.12-arm64-mac.zip`
- 大小：125,809,032 bytes
- SHA-256：`932f6ec9152e1709978bf83eea31a9486648b0527eea474c777b6654158400e8`

## 发布状态

- 已实现、已自动验证、已构建、已安装并完成本机可见验收。
- 开发包为 ad-hoc 签名，不具备正式分发资格。
- 被替换的 `2.0.0-alpha.11` 已移入废纸篓，可恢复。
- 尚未提交或推送；仓库未配置远程，本次也未收到推送指令。
