# alpha.45 白板布局控件验收

日期：2026-08-31

## 复现与修正

- 用户 20:00:11 录屏：结构切换后新容器包住旧的散落坐标，产生重叠和大片空白；批量自动排列只整理组内，不整理顶层容器。
- 浏览器实际重复点击：左右分叉、环绕放射会再次换位。回归数据还复现了多棵独立树按根节点的排列后坐标重新排序导致整棵树换位。
- 结构切换重新应用已选布局；自由摆放例外，保留原可见坐标。手工群组内部偏移、成员和注释保留，后台同步不重排。
- 全局自动排列按内容重新确定紧凑换行宽度，收紧容器并整理全板；单组自动开关和拖动尺寸仍尊重用户给定的宽度。
- 分叉/环绕分支和独立树打包采用稳定次序；固定字号的群组标题限制在容器可见宽度内，低缩放收起内联按钮，点击标题可打开完整工具条。

## 自动与浏览器验证

- `npm run check`：895/895 通过，209 个 JavaScript 文件语法检查通过。
- 新增 `test/relationship-layout-controls.test.js` 20 项：2 台主机、7 个项目、21 个部署、本地仓库、多个访问点和不同卡片高度；包含碰撞、重复排列、结构切换、批量收紧、后台刷新和撤销。
- 使用正式控制器的 `relationship-groups.html?check=layout45b`：三种结构 × 七种布局，共 21 组合，各重复点击两次。控件选中状态正确、重复结果稳定；卡片与独立容器无交叠、组标题无相互遮挡（合法群组包含不作为碰撞）。
- 全局关闭保持可见位置；重新开启后无交叠；保存并重新读取后坐标与尺寸一致；低缩放点击群组标题能显示属性/编辑/排列工具条。

## 打包与安装

- Mac ARM64 开发包、图标校验、源码和产物门禁通过。
- 安装位置：`/Applications/GitFinder 2 Alpha.app`；Info.plist 为 `2.0.0-alpha.45`，与打包产物的 `app.asar` 一致，深度签名验证通过。
- 旧安装与构建保留在 `dist/release-backup-alpha44-before-alpha45/`。
- ZIP：`dist/GitFinder-2-2.0.0-alpha.45-arm64-mac.zip`。本机开发签名，不是正式公证分发包。
- 原生验收文件：`/tmp/gitfinder-alpha45-layout.gitfinder-board.json`，由同一浏览器隔离示例导出，64 个元素，无真实凭据。
- 原生正常启动显示 alpha.45，通过原生打开对话框载入 64 元素的隔离白板。
- 安装版实际点击向右、向下、左右分叉、环绕、分列、自由布局后，UI/隔离文件中的布局字段随之更新。分叉、环绕重复点击的落盘坐标一致；隔离文件保存正常。紧凑布局在首次打开隔离文档时正确呈现。
- 最后一次紧凑按钮点击及原生撤销/重开未完成：Computer Use 报告窗口被改变，随后只读确认应用已重启并回到用户真实白板，布局为左右分叉。停止 UI 操作，没有继续改变真实白板。完整七布局点击、保存重开、撤销分别由上述浏览器矩阵和自动测试覆盖，不把它们等同于这一轮原生验收。

本次未提交或推送；仓库没有配置远程。未更改用户真实白板布局。用户确认有效后再归档修复报告。
