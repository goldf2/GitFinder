# 2.0.0-alpha.37 本机发布验收

日期：2026-08-31。目标：macOS Apple Silicon 本机开发包。

## 提交范围

本次按用户授权整理此前累积的未提交工作，不是从 alpha.36 新增全部功能：

- Coolify 直连、访问点检测、部署与本地仓库匹配及定位。
- 白板文档、项目文件夹及资源复制/引用、ZIP 格式白板包。
- 群组、嵌套、手动尺寸和自动排列；导航、筛选、卡片与面板交互。
- 项目/目录导航、紧凑资源条目、取消收藏夹和文件标签入口。
- 透明背景图标与已有 Windows 打包支持。
- 群组空白区域拖动、标题快捷工具条；按拓扑与位置排序，四向连接点及避障连线。

Web/云同步/在线更新、交付事件与通知备忘只是开发规划；本次不部署云服务、不配置生产 Webhook、不创建凭据。GitFinder 继续直连 Coolify，不读取钥匙串。

## 发布基线

- 整理前 HEAD：`4e7d320`，提交中版本为 alpha.21。
- 整理前工作区源码：alpha.36；系统安装版：alpha.34。
- 本次版本：`2.0.0-alpha.37`；package.json 和 package-lock.json 一致。
- 分支：`main`；没有远程或上游，本次仅本地提交。
- 已检查待提交路径及常见密钥特征，未发现运行数据、数据库或明显凭据混入；不读取应用会话文件。

## 验收状态

| 验收层 | 状态 |
| --- | --- |
| 当前版本的单元测试、语法检查 | 827/827 通过，0 失败；203 个 JavaScript 文件语法检查通过 |
| 图标及源码发布检查 | `verify:icons`、`verify:release-source`、`git diff --check` 通过 |
| macOS App、ZIP、版本和签名检查 | 待运行 |
| 安装前备份、安装及正常启动 | 待运行 |
| 安装版白板交互抽查 | 待运行 |
| Windows 原生安装/运行 | 本次不执行 |

本次使用 ad-hoc 开发签名，不是 Developer ID 签名或 Apple 公证的正式分发包。自动化测试和浏览器夹具不代替安装版真实验收；完成后在本文件追加实际结果。
