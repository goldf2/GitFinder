# 2.0.0-alpha.48 项目星系布局

## 目标

- 在“布局”菜单增加“项目星系”，不改变当前白板结构或关系事实。
- Coolify Project 使用圆形视觉容器，部署完整排列在圆内，独占访问点排列在圆外。
- 多 Project 共享的访问点只保留一个节点，并在相关 Project 附近寻找最近空位。
- 其它布局、Project 归属、关系、备注和锁定状态保持不变。

## 自动验证

- `test/relationship-project-galaxy.test.js` 覆盖圆形边界、部署完整位于圆内、访问点完整位于圆外、共享访问点唯一且不遮挡、重复排列稳定、菜单属性隔离。
- `test/relationship-layout-controls.test.js` 覆盖三种结构与全部非自由布局的混合卡高、双主机、多访问点、重复整理和同步稳定性。
- 全量执行 `npm run check`。

## 浏览器正式控制器验收

- 页面：`scripts/visual-fixtures/relationship-groups.html?galaxy=48`。
- 两 Project 样本：2 个圆形 Project、3 个部署、3 个访问点；卡片无重叠，部署完整在圆内，访问点完整在圆外。
- 密集样本：7 个 Project、49 张卡片；Project 圆、固定字号标题和卡片均无互相遮挡。
- 布局菜单可正常展开并选择“项目星系”；根节点使用 `data-active-board-layout`，不复用菜单事件属性 `data-board-layout`。
- 以上均为隔离数据，不读取或改写用户白板。

## 安装版验收

- 已构建并安装 `/Applications/GitFinder 2 Alpha.app`，界面可见 `v2.0.0-alpha.48`。
- 安装版“布局”菜单可见“项目星系 / Project 内放部署，外围环绕访问点”；仅展开并关闭菜单，未在用户真实白板上应用新布局。
- 构建与安装后的 `app.asar` SHA-256 均为 `34775d9ffabc6cf5f284795d4daf5e4ff5c152b05ab75ecb6749a6ffebfa5781`。
- `codesign --verify --deep --strict` 与开发产物门禁通过；本包仅为 ad-hoc 开发签名，不具备正式分发资格。
- ZIP：`dist/GitFinder-2-2.0.0-alpha.48-arm64-mac.zip`。
- alpha.47 已完整保留在 `dist/release-backup-alpha47-before-alpha48/`。
