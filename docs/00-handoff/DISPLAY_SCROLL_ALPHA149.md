# alpha.149 显示弹窗滚动优化

2026-09-12 +08:00。

- 录屏观察：设置滚动到下方后多次回到图标区域，原访问点 select 保持焦点。焦点导致浏览器回滚为待验证假设，不能单凭录屏认定唯一根因。
- 滚轮/触控板滚动时释放弹窗内原控件焦点，不 preventDefault，不影响默认滚动；阻止滚动传播到白板。
- 固定弹窗表单可用高度、保留底部操作行；内容滚动区禁用滚动锚定、稳定滚动条占位并隔离越界滚动。
- 修复 edgeZoomMode/maxZoom 在恢复默认时未回填表单的遗漏。
- 初次安装验证发现：打开弹窗后后台刷新使整个弹窗消失。补充 render 时保留打开的弹窗 DOM 及滚动位置；避免刷新重建设置控件。顶部改为固定安全边距，避免居中布局在当前容器中裁切标题。
- 最终源码 8420858，1143 项测试、266 个 JavaScript 语法检查及打包通过；安装 Alpha149，保留 Alpha148，codesign 严格校验通过。
- 最终安装截图已确认标题和底部完成按钮处于可见区域。继续点击焦点控件/滚动测试时，CUA 返回用户改变应用状态，之后 AX 不再返回内容、截图 unavailable；进程仍在运行。滚动回跳端到端验收未完成，不标记问题完全解决，不推送。
- 制品 `/Volumes/project/制品与备份/gitfinder-2/2.0.0-alpha.149/`。唯一下一步：重新打开 App 窗口，完成焦点后滚动与后台刷新验收；通过后推送。
