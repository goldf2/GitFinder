# 资源卡片外观预览

2026-09-12（Asia/Shanghai）。用户确认先试大图标效果，本轮收窄为源码预览，未打包、安装、提交或推送。

- 主机：56px 图标底座、40px 机架图标、设备式底边；部署：42px 底座、30px 图标；访问点：圆形地球图标、轻量圆角标签，高度为普通卡片的 60%。
- 缩小到 0.45 以下隐藏次要信息；图标自然随卡片缩放。保留原有状态色、资源关系和 Project 容器；访问操作保留在更多菜单。
- 浏览器打开 `http://127.0.0.1:8765/scripts/visual-fixtures/relationship-flow-engine.html`，使用隔离示例，无用户数据写入；已查看总览和放大截图。
- 预览页面原有全局 header 样式污染卡片头，已限定为页面顶部 header。
- 构建通过。全量检查存在连线路由回归失败（端点高度缩小改变端口方向），尚未完成回归适配，不能称为发布通过；主题、真实安装版以及保存重开尚未验收。

下一步：用户确认图标比例后，补齐尺寸与路由回归，并执行正式交付流程。

## 主机设备造型迭代

用户追问主机后，预览已将主机普通卡片替换为双层机架 SVG，名称置于下方，状态灯位于设备面板，更多菜单位于右角。构建通过，浏览器截图确认 con01 已显示为设备造型。仍仅为预览，未安装或推送；自定义主机图标偏好、主题及完整回归需在正式交付前处理。

## alpha.153 正式实现

用户要求优化 App 后进入交付：保留自定义主机图标覆盖；机架提供暗色外观；部署按真实 runtime.type 的 database/service 字段选择图标；TCP/UDP 使用接口图标。新增访问点尺寸与网页预览回归，原路由测试显式保持旧尺寸，更多菜单测试覆盖设备分支。浏览器实测主机更多菜单可展开并提供详情、显示设置和固定下级。发布状态待安装验收后补记。
