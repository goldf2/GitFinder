# alpha.146 有限连线补偿

2026-09-12 +08:00。用户确认总览不必保持每条连线清晰，不能与卡片争夺视觉注意力。

- 替代 alpha.145 的最小 2px：补偿上限 2 倍。默认线宽在 50%、25%、10%、5% 缩放分别为 1.7、0.85、0.34、0.17 屏幕像素。
- 低于 40% 渐淡；保留原边的透明度比例。虚线随相同补偿系数缩小，箭头随 SVG strokeWidth 缩放。
- 路由、连接对象、显隐配置和布局不变；点击命中区仍保持易操作。
- 1138 测试、266 个 JavaScript 语法检查通过。
- 从提交 cb1726b 打包成功，安装 `/Applications/GitFinder 2 Alpha146.app`，保留 Alpha145；codesign 严格验证通过。
- 安装版已显示 alpha.146；原有白板执行适合内容、连续放大六次及恢复总览，远景连线变细变淡，近景逐步恢复。未调整资源显隐和布局。当前用户白板主要展示主机到项目容器的虚线，箭头缩放由测试及 SVG 默认 strokeWidth 单位覆盖。
- 制品在 `/Volumes/project/制品与备份/gitfinder-2/2.0.0-alpha.146/`。下一步等待用户确认视觉效果；确认后归档修复报告。
