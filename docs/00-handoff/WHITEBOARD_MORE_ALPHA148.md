# alpha.148 点击更多才显示操作条

2026-09-12 +08:00。用户确认卡片和容器使用“⋯”显式打开操作；选中、框选不自动展开。

- 卡片、文字/图片/附件与容器标题均加入更多按钮，保留原有操作。
- 画布共享唯一 openId，与 selection 分离；再次点击同一按钮、执行操作、点击外部、开始拖动或 Esc 关闭。
- 未更改关系、布局或多选高亮行为。源码与安装验证待完成。

## 追加显示配置

- 用户要求连线缩放及最大缩放程度可配置。显示设置加入 edgeZoomMode（adaptive/follow/fixed）与 maxZoom（1/2/4/8 倍），保存到当前白板；旧数据默认有限补偿、8 倍。
- 降低上限后钳制当前视口，重新打开时也限制初始缩放；保留缩小下限 3%。
- 初版安装验证：主机 ⋯ 打开菜单，点击容器 ⋯ 替换主机菜单，外部点击关闭；总览目视只有一条打开的工具条。追加配置安装验证待完成。
