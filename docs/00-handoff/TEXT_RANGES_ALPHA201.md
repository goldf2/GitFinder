# alpha.201 · 扩大文字调节范围

任务：GF-TEXT-RANGE-001。基线 b0e42e9 / alpha.200。

## 目标与边界

用户要求文字调节范围更大，区别于画布缩放。拟将卡片文字85–130%扩展为50–300%、主机/Project标题14–36px改为8–96px、连线/成员说明8–24px改为6–48px、独立文字12–96px改为8–256px。默认值和用户原设置不自动改变，不修改应用全局UI字号。核对控件、读写归一化、持久化和路由/布局字体估计，不能只改滑块。

独立worktree `dist/agentdock-text-ranges-20260919/work`，原10项dirty已备份。保留应用面板命名、Coolify默认浏览器入口、实验进度默认关闭，不读写远端配置，不迁移数据库或批量排列真实白板。

## 初始状态（历史）

开始时检查中、尚未交付；当前交付状态见文末。原始证据存放在 `dist/agentdock-text-ranges-20260919/`，每个阶段按实际结果更新。

## 实施与中间验证

- 首批10项对原版回归1通过/9失败，扩大范围后10/10；后继字体空间测试共13项，需以最终检查为准。
- 扩大上限同时更新模型、显示归一化、表单、独立文字编辑、路由/星系标题估计；没有改变默认字号。
- 实际Electron最大字号复现标题遮挡与卡片底部溢出，已增加新范围下的标题区域和卡片尺寸。旧范围/默认不启用额外标题扩张；新标题只扩展边界，已保存的锁定成员世界位置保持。
- 独立大字文本框至少保留一整行高度，长内容仍沿用滚动；长标题保持省略和悬停原文，不承诺任何长度在任何缩放下全部可读。
- 原CSS快照测试检查旧10px下限，已更新为小字号可到8px且原默认缩放可读下限保持；不是产品逻辑失败。
- 首次UI夹具同时隐藏左右侧栏导致无法点击资源；修正夹具前提后执行。异常退出记录保留，不计为通过。第二次真实UI暴露字体留白缺口；补齐后10项通过，正增加可读缩放下的按钮可见性检查。
- 部分AgentDock请求偶发安全状态无法确定，同一请求原样重试成功；未更改安全设置或换工具绕过。

## 最终源码验收（安装前记录）

13项字号专项通过，完整1381项测试通过，源码11项实际Electron界面检查通过。新增检查覆盖控件/模型端点、旧值/默认、越界处理、表单写入、独立文字、路由估计、真实文件、字体留白、锁定成员及至少一行文本高度。UI覆盖最大/最小字号、可读缩放下的操作可见性、文本256px、独立进程重启以及恢复默认。

新范围导致的尺寸扩展仅作用于更大的字号，旧范围不追加标题扩张/卡片尺寸。真实白板、当前App仍未更新。首轮1378项中旧CSS语法断言失败已按新的小字体下限更新，所有初始失败日志保留。未测试所有字号与所有布局/卡片宽度的任意组合。

## 最终交付 · 2026-09-19T06:40:07+08:00

- 版本 `2.0.0-alpha.201`；精确运行源码 `7ae44cdd3cbca46bc3ee01322889a5d64be6a1ed`，干净工作树1381项测试和295JS通过，开发包门禁issues为空、codesign deep/strict通过。
- 已安装 `/Applications/GitFinder 2.app`，正常用户启动截图 `normal-installed-alpha201.png`。安装ASAR SHA-256：`7cf3e12da28b39e16475254134553c2b0970ce6ccb501dfcb59b49c9203eaa8e`。
- 原版完整备份 `/Volumes/project/制品与备份/GitFinder-2-alpha200-before201.app.disabled`。未生成Windows或正式发行包；只有ad-hoc本机开发签名。回退旧版前应另备份使用了新大字号的白板，旧版会按旧范围处理该值。
- 安装版11项字号交互、32项容器生命周期、12项Coolify入口，共55项全部通过。字段极值、实际计算字号、标题不重叠、可读缩放按钮可见、真实文件写入、离线进程重启、最小值及恢复默认都有记录。
- 原始证据：`installed-text-ranges.log`、`installed-containers.log`、`installed-coolify.log`、各独立UI目录的result.json、`delivery-verification.json`；均位于本任务dist目录，换机需重新验证，不凭日志路径宣称已验收。
- 原3白板文件验收时字节一致、主目录原10项未提交内容按基线保留。未在真实白板替用户选择更大字号或重新排列。
- 源码在安装验收后推送；用户体验确认仍pending。后续记录提交仅更新交接文档，安装制品仍绑定上述运行源码。

## 限定

CDP表单/指针与真实安装渲染、文件IPC测试不等于原生系统鼠标验收；测试不打开真实Coolify后台。没有验证全部布局、文本长度、字号及卡片宽度组合，既有概览模式仍会隐藏卡片辅助信息。高字号会需要更多画布空间，名称过长仍使用省略和悬停原文。
