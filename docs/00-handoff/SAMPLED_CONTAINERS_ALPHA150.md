# alpha.150 采样容器不可解散

2026-09-12 +08:00。用户确认 Project 容器代表采样结构，不应提供本地解散。

- 去掉采样容器 ⋯ 和右键菜单的删除入口，删除执行层也拒绝采样资源/容器。
- 加载拓扑时清理匹配采样容器的旧 dissolved 标记并恢复投影；保留用户显示层级选择及手动分组操作。
- 不连接服务器，不修改部署。AL02 旧标记恢复通过安装版待验证。

## 验证

- 53f985c：1143 项测试、266 个 JavaScript 语法检查及打包通过；采样容器删除拒绝、旧标记恢复、重开保留归属覆盖通过。
- 安装 `/Applications/GitFinder 2 Alpha150.app`，保留 Alpha149，codesign 严格验证通过；AX 确认 alpha.150 与 AL02 · My first project 容器存在。
- 点击容器菜单时 CUA 报告用户持续交互，停止抢占操作。菜单可见性由源码条件覆盖；当前快照中 AL02 实时资源缺失，未将六个部署完整恢复视为已验证。
- 制品 `/Volumes/project/制品与备份/gitfinder-2/2.0.0-alpha.150/`。下一步收集用户对容器恢复的确认；确认后归档修复报告。
