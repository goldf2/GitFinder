# alpha.161 · 主机容器快捷菜单

## 变更

- 主机容器标题旁增加小型 `⋯` 快捷菜单按钮。
- 按钮打开与主机卡片相同的白板节点菜单，包含查看属性、重命名、显示设置和布局等操作；不会触发容器拖动。
- 主机名称区域继续打开显示设置，容器空白区域仍可拖动。

## 验证

- 白板 UI 回归测试覆盖快捷按钮、节点菜单派发和独立显示设置入口。
- alpha.161 安装版验收在打包后完成。
