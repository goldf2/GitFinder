# 类型列表间距与增删改入口 · alpha.137

## 2026-09-10 08:11 +0800

- 用户截图：项目类型行间距过大，类别添加、修改、删除入口不清晰。
- 根因：类型行使用没有布局规则的 `sidebar-shortcut-row`，透明编辑按钮换到下一行仍占位；沿用固定按钮的隐藏样式，编辑入口不容易发现。
- 修复：独立 `project-type-row` 两列横排，类别和编辑按钮同一行，“编辑”常显。新建入口及设置窗口的删除功能继续复用，不改变分类数据。
- 验证：`npm run check` 1117/1117，通过 258 个 JavaScript 语法检查。从提交 `3bc0947` 独立打包，通过 development 发布门禁。
- 安装 `/Applications/GitFinder 2 Alpha137.app`，ASAR 与打包产物一致、严格签名检查通过；旧版 Alpha136 保留。
- 隔离安装版实际点击完成：创建“验收类别” → 编辑为“验收类别已修改” → 删除并确认。创建后列表可见，修改后名称更新，删除后仅原测试类型保留；未删除项目或文件。
- 可见截图与只读几何测量：两行高度均为 28px，行间 1px，编辑按钮 opacity=1 且同一行显示。证据 `/tmp/gitfinder-types-alpha137.fw3mDw/renamed.json`、`deleted.json`、`read-ui.cjs`；测试后已退出隔离配置，启动原用户配置。
- 制品：`/Volumes/project/制品与备份/gitfinder-2/2.0.0-alpha.137/GitFinder-2-2.0.0-alpha.137-arm64-mac.zip`，同目录保存验证报告；SHA256 `20ba055b9a6687e0a4dd803e7c24ab5d2b58d08a08787d97f69cf45929b7763d`。
- 原配置普通启动通过，可见 alpha.137、78 个项目；实际截图确认三种类别紧凑排列，18/13/47 成员数不变，编辑按钮常显。验收通过后随本记录推送；用户确认有效后归档修复报告。
