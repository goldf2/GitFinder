# alpha.203 · 嵌套容器标题防重叠（开发中）

任务：GF-CONTAINER-HEADER-001。基线：alpha.202 / `ee3583340950c71adad37d8fb38ccf0dfdcaa9a5`。
更新时间：2026-09-19T08:59:18+08:00。独立工作区 `dist/agentdock-title-spacing-20260919/work`，主目录和已安装版本尚未改变。

## 用户现象及已复现根因

用户截图：主机 con01 与内部 Project 顶边对齐，标题互相盖住。alpha202 对显式 wrap 容器跳过内容边界，且子容器拖动只遵循extent顶边，没有标题区约束。首批8项测试原版1通过/7失败，草稿8/8通过。此前连接502中断，项目进度未写入；本轮读取确认后补齐记录。

## 当前实现与边界

从内到外为主机和嵌套容器保留标题区，扩大必要外框而非移动已保存内容；直接子项相对坐标重基准，嵌套后代世界坐标不变。拖动使用父标题安全区，容器缩放首行使用同一约束。常规显示不主动迁移用户数据，保存仍需原流程。不同字号、旧尺寸、锁定、连线和重开仍待安装验证，不以单元测试冒充实际验收。

## 验证和下一步

失败到通过日志：`dist/agentdock-title-spacing-20260919/header-before.log`、`header-after.log`。全量检查运行中。唯一下一步：建立复现截图的合成白板，实际测量标题矩形与内部内容边界，验证拖动、缩放、字号及保存重开，再按仓库清单提交/构建/安装/推送。

## 数据保护

原10项dirty和3白板基线已在证据目录记录；仅在独立工作树修改，不操作真实白板，不修改Coolify、凭据或实验功能开关。尚未安装alpha203、未推送、未取得用户验收。

## 恢复后的实际验证与调整

- 实际安装alpha202、隔离同顶边夹具在第一项DOM断言失败：Project顶边进入主机标题区域。证据`installed-alpha202-before.log`；未修改正常用户profile。
- 草稿第一次全量1405/1407：嵌套内边距旧测试期待60px，改为72px后重新核对卡片绝对坐标不变；另一个Git amend夹具在并发全量中未得到HEAD，原样单独1/1通过，未修改Git业务代码，保留失败日志。
- 实际DOM字号验证发现36px沿用旧72px标题区仍有不足，新增失败回归并统一计算中等字号留白；默认20px仍为72px。首次快速测量还遇到React尺寸更新的中间状态，脚本增加稳定等待，保留原失败几何，不把它单独当作已证实的常驻重叠。
- 标题区从内到外补足，锁定内部卡片世界坐标不变；手工多层嵌套和保存模型往返补充单元覆盖。嵌套长标题使用容器可用宽度，不再被悬浮标题的280px固定上限截短；仍保留超长内容省略与悬停原文。
- 最新源码UI一次28项通过，含5种字号×3种缩放的DOM矩形、实际拖动期间与松开后的边界、缩放、撤销重做、真实磁盘读回和进程重启。源码版alpha202字样的早期截图是尚未递增版本的开发验证，不是安装交付。
- 追加测试前全量1411/1411、298JS通过；最终确切提交门禁仍待执行。源码容器38项相邻回归运行中。没有安装或推送alpha203。

## 可复跑入口

```bash
node --test test/container-header-spacing.test.js
npm run check
node scripts/verify-container-headers.js
node scripts/verify-container-headers.js --app '/Applications/GitFinder 2.app/Contents/MacOS/GitFinder 2 Alpha'
```

验证脚本只操作自己的profile和合成资源。字号使用真实表单处理，拖动用Chromium CDP指针，保存用真实IPC/文件读回；不冒充系统辅助功能原生鼠标、真实Coolify账号或全部规模组合。
