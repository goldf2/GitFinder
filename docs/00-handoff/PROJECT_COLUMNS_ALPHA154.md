# alpha.154 · Project 纵向列表

2026-09-12（Asia/Shanghai），用户确认新增显示布局，不改变真实资源关系。

- 布局菜单新增 `Project 纵向列表`（project-columns）。主机按名称分列，关联 Project 按名称纵向排放；同名以稳定 ID 排序，不依赖旧坐标。
- 沿用完整群组移动、锁定保护和访问点邻近排列。多主机共享容器只放一次；没有可见主机归属的资源进入独立列；不新增或更改 groupId。
- `npm run check`：1151 测试通过，268 个 JavaScript 文件语法检查通过。包括新增分列/排序/重复稳定测试以及已有所有布局与结构交叉测试。
- 下一步：安装验收菜单、真实容器顺序与重开持久化；未验收前不声明已交付。
