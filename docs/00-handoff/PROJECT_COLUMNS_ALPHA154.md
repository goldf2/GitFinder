# alpha.154 · Project 纵向列表

2026-09-12（Asia/Shanghai），用户确认新增显示布局，不改变真实资源关系。

- 布局菜单新增 `Project 纵向列表`（project-columns）。主机按名称分列，关联 Project 按名称纵向排放；同名以稳定 ID 排序，不依赖旧坐标。
- 沿用完整群组移动、锁定保护和访问点邻近排列。多主机共享容器只放一次；没有可见主机归属的资源进入独立列；不新增或更改 groupId。
- `npm run check`：1151 测试通过，268 个 JavaScript 文件语法检查通过。包括新增分列/排序/重复稳定测试以及已有所有布局与结构交叉测试。
- 下一步：安装验收菜单、真实容器顺序与重开持久化；未验收前不声明已交付。

## 本次交付状态

- 本地提交 e68a68f，隔离构建 `/tmp/gitfinder-alpha154-build` 成功，codesign 验证通过。
- 安装并普通启动 `/Applications/GitFinder 2 Alpha154.app`，保留 Alpha153。操作前配置与白板备份 `/tmp/gitfinder-before154.hfrRtX/`（仅本机，含配置，不提交）。
- 安装版 AX 确认 alpha.154 与新增菜单项。原白板仍为左右分叉；点击菜单和键盘选择没有改变选中值，截图返回 unavailable。不能声称纵向排列已实际应用或可见验收通过。
- 暂未推送，遵循安装交互验收后推送规则。下一步恢复界面控制或由用户选择“布局 → Project 纵向列表”确认真实显示，再完成推送。

## 用户实际视图复查

用户反馈排列不理想后，本轮只读检查安装版，确认当前布局已是 Project 纵向列表。截图显示三个纵向区域但整体极小，若干 Project 内部大面积空白，纵向最长的一列决定全局缩放。

代码证据：控制器 `_arrangeCurrentLayout` 固定传 `preserveGroupContents: true`，排列器因此保留旧成员分散位置并合并旧容器边界，无法缩紧历史空白；容器按列最大宽度居中，不是左边缘对齐；全局适合内容又受长列高度约束。此前测试使用规整的小容器，未覆盖历史膨胀容器，不足以证明真实白板的紧凑可读性。

下一步建议：仅对显式自动排列的未锁定 Project 做内部紧凑重排与边界收缩，再做列内左对齐；纵向列表以可读缩放浏览，保留独立的全局适合内容入口。需要实际历史布局回归和可见对比后再交付。本轮未修改实现或操作用户布局，继续暂缓推送。

### 继续诊断的补充证据

- 只读合成复现：包含一张 260×120 卡片、历史尺寸 2400×6000 的自动容器，在 preserveGroupContents=true 时仍保留大框；关闭该选项后可缩至 316×202。不同宽度组居中排列导致 x 分别为 1080 和 80，说明不是左对齐。
- `_setLayout` 在非自由布局下自动 fitContent；`fitContent`（控制器 6417 起）使用 `Math.min(0.03, minZoom)`，即使调用方要求更高的最小缩放，也被压到不高于 3%。修正阅读视口不能只新增调用参数。
- 这是布局质量问题，未见数据读取缺失证据。本次不修改布局实现，不打包或推送；alpha.154 仍未通过用户实际布局验收。
