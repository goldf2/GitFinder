# 项目类型与目录层级 · alpha.136

## 2026-09-10 · 已确认方向与实现

- 用户要求：主区不再展示大型项目组分类；左侧单一“项目类型”入口承载分类筛选，真实父子项目按目录层级展示。
- 保留现有 `projectGroups` 数据和稳定 ID，仅调整语义及界面；类型可以交叉分类，不产生目录父子关系、不移动文件。
- 左侧项目类型默认折叠，展开后选择类型、未分类或编辑类型；创建/删除类型保留在此入口的设置中。主区只有匹配项目，不重复展示分类卡片。
- 项目目录树按最近的已项目化父目录嵌套；未项目化的中间文件夹不是项目节点；子项目的仓库不重复出现在父项目下。
- 类型筛选写入已有 contentQuery，随标签页保存/恢复；“所有项目”清除类型条件，目录浏览不继承分类筛选。
- 选择项目目录时同步右侧项目详情，使用既有详情展示，不改变文件选择或目录内容。
- 验证：首轮 39 项专项回归通过；安装测试暴露目录异步渲染会覆盖项目详情，追加回归修复后最终 `npm run check` 1116/1116，258 个 JavaScript 文件检查通过。
- 本次承接原工作区已有的项目组侧栏草稿，其他 README、项目元数据、历史交接文档等未提交修改保持原状。
- 用户确认有效后归档修复报告；目录首次启动时项目扫描和详情初始化的先后仍可能出现空详情，当前已验证的是点击项目后、目录异步渲染完成后的详情保持。

## 2026-09-10 08:04 +0800 · 安装验收

- 版本 `2.0.0-alpha.136`，从已提交源码 `daf1d64` 的独立工作副本打包，发布门禁通过（本机开发签名）。
- 安装 `/Applications/GitFinder 2 Alpha136.app`；Alpha135 保留。已安装 ASAR 与产物完全一致，严格签名检查通过。
- 隔离配置建有三个实际项目文件夹：父项目、跨普通中间目录的子项目、独立项目。实际点击验证：主区分类卡片为零；创作类显示两个成员；未分类仅显示子项目；父项目展开只显示其真实子项目；点击子项目后右侧详情在异步渲染结束后仍在。
- 正常退出重开后，“未分类”查询保留，仍只显示一项；项目类型入口默认折叠。测试不改动真实项目分类或文件夹。
- 证据目录 `/tmp/gitfinder-types-alpha136.HVpxgY/`：`check-final.log`、`pack-final.log`、`read-ui.cjs`、`unclassified.json`；可见界面通过应用操控工具验证。
- 制品：`/Volumes/project/制品与备份/gitfinder-2/2.0.0-alpha.136/GitFinder-2-2.0.0-alpha.136-arm64-mac.zip`；同目录保存验证报告。
- ZIP SHA256：`9f1023e0254e57b44e31d9a2551072fcf27c8eed586e19251e0d2319f9f0ea4f`。
- 已结束隔离测试，普通启动原用户配置成功：78 个项目，项目类型保留创作类 18、销售类 13、管理类 47，未分类 0；主区不再出现分类大卡片。验收通过，提交本记录后一并推送 `origin/main`。
