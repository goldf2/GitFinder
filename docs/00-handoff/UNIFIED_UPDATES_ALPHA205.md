# 统一更新接入 alpha.205

2026-09-20T06:00:37+08:00：用户明确授权接入并发布GitHub/官网。基线95a5408、alpha.204；独立worktree保护主目录既有修改。实现进行中；本机0个有效Developer ID身份，正式Mac签名公证尚不可执行。未访问AL02或更改用户白板。

首次源码测试1412/1413，失败为既有/tmp符号链接路径用例；按项目已有规范使用TMPDIR=/private/tmp后1413/1413与298JS检查通过。新增统一协议测试后将重跑。共用模块5项测试通过；开发包走明确手动安装，正式签名版继续调用原生安装器。尚未打包或安装。

优化后的源码验证：TMPDIR=/private/tmp npm run check通过1418/1418测试及299JS；其中5项是共享协议专项。将从本次源码提交构建可追溯开发包，尚不具备正式签名资格。

## 2026-09-20 CI 后续候选 alpha.206

alpha.205 的 GitHub Actions 35472928510 未通过：macOS 测试比较了临时目录非规范路径；Windows 检查未兼容 CRLF。修复为比较 realpath 和仅规范化换行，不放宽台账内容校验。新候选 alpha.206，正式签名、公证及公开发布仍待完成。

alpha.206 Mac CI 通过；Windows 的台账直接调用测试使用非 native realpath，和服务的 Windows 原生长路径不同，7 项用例无项目。fixture 改为原生规范路径，与真实调用方一致。候选 alpha.207，需重新跑 Windows CI。
