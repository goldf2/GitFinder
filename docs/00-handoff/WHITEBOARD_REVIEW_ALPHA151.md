# alpha.151 白板整体检查后的修复

2026-09-12 +08:00。用户确认开始实施整体审查的前三项和文字显示设置。

- 根因：实时投影 addEntity 排除了已有持久化节点，导致已保存主机/部署缺少实时 runtime，且被误报为实时资源缺失。改为实时投影保留这些身份，本地实体与位置仍不覆盖。
- 资源库保持主机层级展开；部署/访问点分类计入下级资源，并解释展开入口，避免下级资源被误计为零。
- 采样 Project 及其成员不进入手工编组、移出成员列表，归组执行检查也拒绝改写采样归属。
- 显示设置增加标题随缩放程度、连线说明字号、成员说明字号；标题和更多按钮一并缩小，展开操作菜单恢复可读尺寸。布局和路由的标题占位增加更多按钮宽度。
- 源码验证：npm run check，1145 项测试通过（新增保存采样节点后实时状态保留、显示字段持久化回归，扩展采样成员保护检查）。
- 安装验收待完成；状态文案细分和自动排列开关命名暂未改动。
