# GitFinder 2 下一步

<!-- BEGIN NEXT_ACTIONS -->
## 下一任务：GF-AI-PROGRESS-001

更新时间：2026-09-18T09:43:56+08:00。GF-COOLIFY-LINK-001的alpha198入口、默认浏览器委托、安装和推送已完成；无需重复发布源码或重建原白板。

用户使用：点击部署卡片底部、主机或Project标题栏的Coolify按钮，由系统默认浏览器打开对应后台。缺来源时禁用，不拿网站或首页替代；用户实际浏览器/账号验收仍pending。

下一开发仅设计AI辅助进度的证据输入、建议/人工确认和受控回写，不自动启用模型或上传源码。容器删除/分组生命周期诊断仍未在本轮解决，GF-QUALITY-001规模矩阵保留待做；实验进度继续默认关闭。

复跑、备份和白板校验限定见[链接交付记录](COOLIFY_CARD_LINKS_ALPHA198.md)，任务以[唯一台账](../../management/development-tasks.json)为准。
<!-- END NEXT_ACTIONS -->

## 历史兼容记录（以下旧下一步已被顶部接续块取代）

## 2026-09-16 · alpha.193 已联合交付

- 布局菜单、均衡主机总览与alpha.192图片生命周期修复已一起安装验收并推送；没有额外安装独立192版。
- 下一步：用户在原关系白板选择“布局 → 均衡总览”确认体验；可用撤销恢复，或切换自由摆放保留位置。确认有效后归档支持文档修复报告。
- 不自动应用新布局，不迁移数据库，不执行服务器写操作。安装/重启、数据边界和可恢复备份统一见 [LAYOUT_OPTIONS_ALPHA193.md](LAYOUT_OPTIONS_ALPHA193.md)。

## 2026-09-16 · alpha.191 已交付

- 保存生命周期已抽离，源码 `fc72a86` 已推送；1224项检查、安装33项流程与重启4项通过，原用户数据保留。
- 下一步收集连续编辑、取消另存为和切换白板反馈，确认有效后归档修复报告；下一开发切片优先附件/图片保存回归，不改变数据格式或扩大数据库迁移。
- 本轮未覆盖真实磁盘故障、原生文件选择器取消、Windows或正式分发；上轮单次全屏偏差未计作已修复。详见 [SAVE_LIFECYCLE_ALPHA191.md](SAVE_LIFECYCLE_ALPHA191.md)。

## 2026-09-16 · alpha.190 已交付

- 已完成保存状态/宽度记忆/拖动性能与测试发现范围修复，1212 项检查、安装版 19 项交互和重启宽度恢复通过；源码 `4777405` 已推送。
- 下一步仅收集实际使用反馈；一次源码全屏测试偏差已保留轨迹采集入口，出现相同现象时再复现诊断，不凭单次通过宣称全场景稳定。
- 支持文档修复报告待用户确认后归档。继续暂缓数据库迁移、服务器写操作、大规模重构及 Windows/正式发布。详见 [OPTIMIZATION_ALPHA190.md](OPTIMIZATION_ALPHA190.md)。

## 2026-09-15 · alpha.189 已交付

- 已完成隔离构建、正常安装启动、安装版右栏交互与退出重启宽度恢复；源码 `c824659` 已推送并远端核对。
- 下一步仅收集用户实际拖动右栏左边缘后的反馈，确认有效再归档支持文档修复报告；不重复安装或重做数据库/白板布局。
- 边界与可恢复备份见 [RIGHT_PANEL_WIDTH_ALPHA189.md](RIGHT_PANEL_WIDTH_ALPHA189.md)。

## 2026-09-15 · alpha.188 验收接续

- 唯一下一步：全量检查已通过，继续可追溯 macOS 开发包及安装版全屏/菜单验证；通过后再推送本次源码。
- 保留所有既有未提交文档与 `.gitfinder/project.json`。不做 Windows、商店发布、数据库迁移或服务器写操作。
- 详见 [WHITEBOARD_LIFECYCLE_ALPHA188.md](WHITEBOARD_LIFECYCLE_ALPHA188.md)。

更新时间：2026-09-07 06:38:00 +0800。

当前交付 alpha.114（已构建并替换本机）：新增本机脱敏 Coolify 同步日志和白板“日志”面板；无新日志时也直接显示本地拓扑缓存的实例 / 服务器 / 部署数量，不触发网络请求。面板显示最近运行、实例结果、各端点成功 / 失败次数、耗时、响应数量、读取计数和错误，并可定位日志文件；不显示令牌、完整 URL 或原始响应。只读检查已确认 Con01 / AL03 当前端点返回 200；旧版缓存为 3 个实例、3 台服务器、35 个部署、无同步错误，“3 个最近失败”属于部署历史事实。下一步仅需用户点击“日志”确认缓存摘要，首次新版同步后再确认逐请求记录，不额外探测 AL02。

当前交付 alpha.110：已修复 Coolify 请求永久挂起、provider 超时不取消底层请求，以及断开 / 更新后迟到结果污染白名单的问题。已完成全量测试、macOS arm64 打包、本机替换和启动验证。遇到单个不可达实例时，白板应在有界时间内回到“已同步但有实例失败 / 缓存”状态，不再无限停在“后台同步中…”或表现为闪退。

下一步仅需用户在本机重新打开关系白板并确认：1) 进程不退出；2) Coolify 状态最终显示成功或明确的实例失败；3) 已有拓扑 / 缓存不被清空。若仍出现空白，记录状态栏完整文案和发生时间，再针对新的错误路径继续处理。不要重复构建已完成的 alpha.110，也不要把缓存状态误认为线上全量成功。

当前交付 alpha.108（已打包、替换本机并推送）：本机工作区默认只显示已固化节点；运行拓扑的主机 / Project / 部署 / 仓库范围是在线预览，确认后可一次性加入本机工作区并保存无凭据节点、布局和关系。资源库动态实体的添加 / 定位 / 拖入已统一解析。Coolify 拓扑、绑定、关联和本地仓库检查已解耦，拓扑先可用；拓扑首屏最多读取最近更新的 8 个部署历史，其余详情不阻塞；部分失败显示可用实例比例和安全网络错误码。alpha.108 已安装，版本 / ASAR / codesign 已核对；只剩 Mac 解锁后的可见白板验收。

当前验收重点：打开本机工作区不应自动增加在线卡片；在运行拓扑中选一台主机或一个 Project，确认预览后点击“将当前范围加入本机工作区”，节点、关系和布局应固化且状态回到“当前白板”。刷新 Coolify 时应先显示拓扑，不等待本地仓库扫描；成功实例仍显示，失败实例显示安全错误码和旧快照，不清空现有白板。若 AL02 再次失败，记录 TCP / TLS / 反向代理证据。

当前交付 alpha.105：白板改为单画布，运行拓扑与代码架构作为可共存的独立元素来源；菜单改为“运行拓扑 / 代码架构 / 布局”，各自拥有显示、范围和结构控制。Coolify 继续使用线上源优先、局部失败保留实例缓存、全失败回退本地无凭据快照。`npm run check` 1057/1057 与 `npm run pack` 已通过；macOS arm64 包已替换本机安装版，提交 `228b261` 已完成本地提交。下一步：推送并在 Mac 解锁后做可见白板验收。

当前验收重点：在同一白板打开运行拓扑与代码架构，分别切换范围，确认架构边界隐藏不影响运行资源；制造 Coolify 单实例超时或全局不可达时，确认已有拓扑不被清空且状态标记为缓存 / 陈旧。确认有效后，按规则归档支持文档修复报告。

当前交付 alpha.104：修复代码架构层添加运行资源后“添加无效”的层过滤问题；Coolify 改为部分失败保留旧快照、全失败优先使用本地快照，状态栏区分后台刷新。`npm run check` 1056/1056 通过；macOS arm64 development 包已构建并替换本机安装版，等待用户复核“添加到白板”和断网/超时后的缓存提示。

本地 / 线上同步规则：Coolify 线上数据成功刷新后写入本地无凭据快照；网络超时或单实例失败时不删除本地事实，显示缓存 / 陈旧状态，并保留错误提示。不要把缓存状态误认为线上已同步。

当前交付 alpha.103：运行拓扑与代码架构已拆为独立数据层，移除合并视图；“范围”菜单提供当前白板、全部资源、单主机、Project、部署、仓库，以及当前 Archify 快照、边界、组件邻接的选择。默认不会把所有项目注入白板。`npm run check` 1053/1053 通过；macOS arm64 development 包已构建、替换本机安装版并核对为 alpha.103，待推送当前提交及本次发布记录。

代码架构当前为空不是入口缺失：本机仓库还没有 `.gitfinder/architecture/` 快照。下一步在仓库详情导入 Archify JSON，再在关系白板选择“结构 → 代码架构”；导入后可按快照、边界或组件邻接缩小显示范围。

当前交付 alpha.102：资源库点击白板文件、项目、仓库会在右侧摘要面板显示内容；白板文件可打开，项目/仓库可添加到当前白板；仓库列表中的项目目录会显示项目颜色。Archify 快照已接入只读架构层。macOS development 包已构建并替换本机安装版，`npm run check` 1049/1049 通过。下一步只需用户在实际白板点击确认；确认后归档 `/Volumes/project/支持文档/修复报告/` 修复报告。不要重复交接或重复构建。

当前立即事项 GF-DRAG-20260905-05：alpha.97 已可恢复替换、本机键盘联动与重开验收通过、ff5772a 已推送。鼠标自动化未产生位移，需用户实际拖动确认；不要把键盘证据称为鼠标通过。用户确认有效后归档支持文档修复报告；不要重复提交/构建/替换。

产品方向新增 GF-ARCH-20260905-01：白板作为真实资源拓扑与代码架构入口。下一阶段先设计“运行拓扑 / 代码架构 / 合并视图”三种只读视图，接入 `goldf2/archify` 的快照分析；明确来源、提交号、生成时间、验证状态和离线缓存，再考虑跨层代码组件关系。Archify 的分析结果不能覆盖 Coolify 或本地扫描得到的运行事实。

隔离证据：`/Volumes/project/临时文件/gitfinder-hidden-repo-drag-alpha97.ghPrjB/README.md`，含鼠标未移动记录、键盘 after 和重开 reopen。隔离实例已正常退出，用户原配置已恢复；无调试参数运行 alpha.97。

最新交付 alpha.96：共享域名访问点浮动、红线与左上角警报，已打包替换本机并推送。下一步等待用户实际白板确认；确认有效后按 GF-ENDPOINT-20260905-04 在支持文档修复报告归档。本轮未启动 Windows 或商店公开发布。

当前优先项：独立 Casdoor 组织 `gitfinder`、Native 应用 `gitfinder-desktop` 与本机 alpha.97 配置已完成，浏览器已打开正确的 GitFinder 2 登录页。Email 注册项已可见且必填，但 17:21 获取验证码明确报错：`gitfinder-desktop` 应用的提供商列表没有 Email Provider。下一步复核全局邮件提供商是否已保存及 SMTP 配置是否完整，然后将它绑定到 `gitfinder-desktop`、发送测试邮件。Phone 目前也为必填，如不配置短信提供商就应取消 Phone 必填；若需要直接使用邮箱登录，还需开启“邮箱作为用户名”并重新整理注册字段。之后执行真实注册、回调、令牌刷新、退出及普通重开验收。正式域名 `auth.oaktechz.com` 仍待恢复，在此之前保持使用当前可用 issuer；不得把 Client Secret 或邮件凭据写入桌面端、Git 或文档。

线宽功能 alpha.95 已通过安装版实时调节和正常退出重开持久化；用户可在“显示设置 → 画布与关系”使用，不需要重新排列白板。

1. **确认 alpha.94 在实际白板中的体验**：上级开启“固定下级”后，Project 容器及内部成员整体跟随，尺寸不变，保存重开后位置保留。工程与安装版鼠标验收已完成；用户确认有效后按 GF-DRAG-20260905-03 归档修复报告。
2. **跟进 alpha.93 的异步修复反馈**：目录快速折叠不回弹、后台刷新不打断拖动、关闭重开无旧数据覆盖。这些修复已包含于 alpha.94；用户确认后按 GF-ASYNC-20260905-02 归档。
3. **已有并行改动独立收口**：Apple 界面、更新和 CI 工作仍未提交，不属于本次 alpha.94 提交。后续按各自任务验收交付。

长期候选计划见 [旧交接未完成事项](../ai-handoff/open-work.md)，旧版本和发布快照仅供历史参考；当前事实以 [CURRENT_STATE.md](CURRENT_STATE.md) 为准。本轮未启动 alpha.94 商店公开发布或 Windows 构建。
