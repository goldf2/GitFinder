# GitFinder 2.0 领域上下文

## 核心定位

GitFinder 2.0 是开发者本机上的项目与部署关系中心。它管理本地事实和显式关联，但不成为服务器监控的唯一执行者。

## 领域词汇

- **本地项目**：由稳定 `projectId` 标识的聚合容器，可包含零个、一个或多个 Git 仓库。
- **Git 仓库**：项目或普通目录上的可叠加属性，不自动构成项目。
- **Provider**：向 GitFinder 提供部署目录、状态快照和事件的受信数据源。Alpha 的首个 Provider 是 Xiangshu Panel。
- **部署资源**：Panel 返回的可稳定引用对象，如应用、服务或数据库。
- **部署关联**：用户明确建立的“本地项目/Git 仓库 ↔ Provider 资源”关系，不由名称或仓库 URL 自动写入。
- **服务器事实**：由 Panel 观测并带有时间、来源和稳定身份的状态、域名、环境和运行位置。
- **事件**：Panel 根据连续观测生成的状态变化记录，必须有稳定 `eventId` 或等价去重键。
- **通知策略**：项目、环境、严重程度、免打扰、冷却、恢复通知和连续失败阈值的本机偏好。

## 权威与数据边界

- 本地项目身份、目录与 Git 事实由 GitFinder 权威管理。
- Coolify 节点、资源状态、域名和监控事件由 Xiangshu Panel 权威管理。
- 服务器端持续监控不依赖 GitFinder 进程；电脑休眠或应用退出不能中断 Panel 的监控。
- GitFinder 只消费快照与事件，不将“本机未收到通知”解释为“服务器未监控”。
- 便携文件只允许非敏感稳定标识；Token、Cookie、私钥、授权头和凭据派生值不得进入项目目录、白板、日志或导出。

## Alpha 非目标

- 不复制 Xiangshu Panel 页面或其服务器凭据体系。
- 不直接依赖 Panel 公开页面的 DOM、截图或未版本化接口。
- 不开放停止、重启、删除、部署或服务器配置写操作。
- 不建立独立一级“部署视图”。

