# GitFinder 2.0 领域上下文

## 核心定位

GitFinder 2.0 是开发者本机上的项目与部署关系中心。它管理本地事实和显式关联，但不成为服务器监控的唯一执行者。后续目标是贯通开发、提交、构建、部署、运行和访问检测，详见[全流程可视化与通知方案](docs/product/delivery-observability-notifications-plan.md)；方案中的 M1–M4 尚未交付。

后续增加 Web 端及用户选定白板的云同步，并独立推进在线版本分发，详见[Web、云同步与在线更新开发备忘](docs/product/web-cloud-release-memo.md)。本地目录与源码处理仍由桌面端负责；该备忘是开发方向，不代表云服务或在线升级已交付。

## 领域词汇

- **本地项目**：由稳定 `projectId` 标识的聚合容器，可包含零个、一个或多个 Git 仓库。
- **Git 仓库**：项目或普通目录上的可叠加属性，不自动构成项目。
- **Provider**：向 GitFinder 提供部署目录与状态事实的受信数据源。当前直接连接 Coolify，不依赖 Xiangshu Panel；事件推送是独立的后续能力。
- **部署资源**：Coolify 返回的可稳定引用对象，如应用、服务或数据库。
- **部署关联**：项目中保存的显式绑定优先于本机手工选择；没有显式绑定时，可以根据完整源码地址与本地当前 origin 的唯一匹配生成自动连线。自动结果不写入项目文件，不按名称猜测，多副本需用户确认。解除自动关联后，本机记住暂停状态。
- **服务器事实**：由 Coolify API 返回并带有时间、来源和稳定身份的状态、域名、环境和运行位置；本机 HTTP 检测是另一种独立观测。
- **事件（规划）**：从部署历史或经过验证的 Webhook 整理的过程/状态变化记录，需有稳定 ID 或等价去重键；API Token 不代表已经订阅推送。
- **通知策略（规划）**：项目、环境、严重程度、免打扰、冷却与恢复通知等本机偏好；明确的部署失败与需要连续失败阈值的 HTTP 探测分别处理。

## 权威与数据边界

- 本地项目身份、目录与 Git 事实由 GitFinder 权威管理。
- Coolify 资源配置与部署事实由 Coolify 管理，GitFinder 只读获取；运行状态、最近部署结果、HTTP 检测和数据源同步状态分别表示。
- 配置提交、最近部署提交与当前运行版本不是同一事实；缺少运行版本证据时保留未知。
- 本机检测依赖 GitFinder 运行，不等于服务器端持续监控。后续可选 Webhook 接收服务只保存和转发事件，也不等于持续探测器。
- GitFinder 不将“本机未收到通知”解释为“服务器未监控”；常在线接收服务的生产配置与部署须另行确认。
- 便携文件只允许非敏感稳定标识；Token、Cookie、私钥、授权头和凭据派生值不得进入项目目录、白板、日志或导出。

## Alpha 非目标

- 不复制 Xiangshu Panel 页面或其服务器凭据体系。
- 不直接依赖 Panel 公开页面的 DOM、截图或未版本化接口。
- 不开放停止、重启、删除、部署或服务器配置写操作。
- 不建立独立一级“部署视图”。
